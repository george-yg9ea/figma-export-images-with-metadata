{
  "name": "figma-export-retain-metadata",
  "version": "0.1.0",
  "private": true,
  "description": "Figma plugin to export cropped images while retaining original metadata (JPEG-first).",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "1.89.0",
    "esbuild": "0.21.5",
    "typescript": "5.4.5"
  },
  "dependencies": {
    "@types/exif-js": "^2.3.1",
    "exif-js": "^2.3.0"
  }
}
